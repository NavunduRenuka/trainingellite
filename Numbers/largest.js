let a=20, b=15, c=30;
let largest;
if(a>=b && a>=c) {
    largest = a;
   }
      else if(b >= a&& b \
        }"">=c) {{?|
        
          largest =b;
      }
    else{
        largest = c;
    }
        console.log("the largest number is:" + largest);